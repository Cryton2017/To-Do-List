<!DOCTYPE html>
<html>
  <head>
    <%= stylesheet_link_tag "main.css" %>
    <%= stylesheet_link_tag "newItem.css" %>
  </head>
  <body>
    <% if current_user %>
      <div class='header'>
        <div class="left">
          <h2>Todo List</h2>
        </div>
        <div class="right">
          <p>Logged in as <%= current_user.email %>.</p>
          <%= button_to "Log Out", logout_path, method: :get, :class => "logoutBTN" %>
        </div>
      </div>
      <div class='content'>
        <div class='newItemCont'>
          <h1>New Item</h1>
          <%= render 'form', to_do: @to_do %>
          <%= button_to 'Back', dashboard_path, method: :get, :class => 'navBTN' %>
        </div>
      </div>
    <% else %>
      <%= lredirect_to root_path %> 
    <% end %>
  </body>
</html>


